# .env.example
# Environment variables for the ATM Smart Dispatch System

# Elasticsearch URL
# The docker-compose setup uses this hostname.
# For local development outside of Docker, you might use http://localhost:9200
ELASTICSEARCH_URL=http://elasticsearch:9200

# Redis URL
# The docker-compose setup uses this hostname.
# For local development outside of Docker, you might use redis://localhost:6379
REDIS_URL=redis://redis:6379

# To use these settings, copy this file to a new file named .env
# and customize the values as needed.
